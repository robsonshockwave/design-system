{
  "name": "design-system",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@8.4.1",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.3"
  }
}
